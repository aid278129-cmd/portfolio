// useScrollPath — legacy hook, superseded by the Player-Possession system.
// Kept for reference only; no longer imported anywhere.
// The ball now follows PLAYER_CHAIN positions in Ball.jsx instead of a CatmullRom path.
export function useScrollPath() {
  return {}
}
